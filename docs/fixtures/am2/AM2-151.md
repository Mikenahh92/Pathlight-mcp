# TC-04A Group Auto-Approve Gating (Pending User Proposal) — Auto-mode v2 E2E

- Fixture ID: AM2-151
- Test case: TC-04 subcase A — group auto-approve gating via a pending user proposal
- Story: AM2-151 (TC-04A member 2 fixture)
- Group: GRP-3 ("TC-04A auto-approve gating pair") — member 2 of the grouped
  pair; sibling AM2-150 (member 1), shared worktree/branch

Fixture payload for the auto-mode v2 grouped-story run: subcase A exercises the
group auto-approve gate when a pending user proposal holds the chain — member
2's work lands on the shared GRP-3 branch behind that gate rather than
auto-merging straight through. This note is the light, verifiable artifact each
stage carries forward.

## Verification

File presence at `docs/fixtures/am2/AM2-151.md` ⇒ fixture applied.

## Status

fixture — do not delete manually
